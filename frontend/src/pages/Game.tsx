import React, { Component } from "react";
import Wrapper from "../components/wrapper";

export default class Game extends Component
{
    render()
    {
        return(
            <Wrapper>
                This will be game area
            </Wrapper>
        );
    }
}